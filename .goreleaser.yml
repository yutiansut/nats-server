---
project_name: gnatsd

release:
  github:
    owner: nats-io
    name: gnatsd
  name_template: 'Release {{.Tag}}'

builds:
- goos:
  - linux
  - darwin
  - windows
  goarch:
  - 386
  - amd64
  - arm
  - arm64
  goarm:
  - 6
  - 7
  ignore:
    - goos: darwin
      goarch: 386

  main: .
  ldflags: -s -w
  binary: gnatsd
  lang: go

archive:
  wrap_in_directory: true
  name_template: '{{ .ProjectName }}-v{{ .Version }}-{{ .Os }}-{{ .Arch }}{{ if .Arm
    }}v{{ .Arm }}{{ end }}'
  format: zip
  files:
  - LICENSE
  - README.md

nfpm:
  formats:
    - deb
  name_template: '{{ .ProjectName }}-v{{ .Version }}-{{ .Os }}-{{ .Arch }}{{ if .Arm
    }}v{{ .Arm }}{{ end }}'
  bindir: /usr/local/bin
  description: A High Performance NATS Server written in Go
  vendor: nats.io
  homepage: https://nats.io
  license: MIT

snapshot:
  name_template: SNAPSHOT-{{ .Commit }}

checksum:
  name_template: '{{ .ProjectName }}-v{{ .Version }}-checksums.txt'

dist: dist

github_urls:
  download: https://github.com
